#include<stdio.h>            // 1. ไม่มีเครื่องหมาย # เเละ ไม่มีเครื่องหมาย <>

int main(){
    int a = 10;             // 2. ไม่มีเครื่องหมาย ;
    printf("a = %d\n",a);   // 3. ไม่มีเครื่องหมาย ; เเละ ไม่มีเครื่องหมาย \n
    return 0;               // 4. ไม่มีเครื่องหมาย ;
}